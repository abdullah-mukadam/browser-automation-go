# Environment Configuration for Browser Automation System

# API Server
PORT=8080

# MySQL Database
MYSQL_DSN=automator:automator@tcp(localhost:3306)/automator?parseTime=true

# Temporal
TEMPORAL_HOST=localhost:7233

# Ollama (Local LLM)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=codellama:13b

# LLM API Keys (Optional - leave empty if not using)
# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4-turbo-preview

# Anthropic (Claude)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-sonnet-20240229

# Google Gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-pro

# Worker
SCREENSHOT_DIR=/tmp/screenshots

# Browser Display Mode
# Set to 'false' to enable VNC viewing of browser automation (connect to port 5900)
# Set to 'true' (default) for headless mode
HEADLESS=true
